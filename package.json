{
  "name": "dex-cpmm-project",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
  "hhnode": "hardhat node",
  "compile": "hardhat compile",
  "deploy:local": "hardhat run scripts/deploy.js --network localhost",
  "setup:local": "hardhat run scripts/setup.js --network localhost",
  "start:local": "hardhat run scripts/start.js --network localhost",
  "bootstrap:local": "node scripts/bootstrap.js",
  "backend": "node backend/server.js"
},
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.1.0",
    "hardhat": "^2.22.10"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "ethers": "^6.13.2",
    "express": "^4.19.2"
  }
}